{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main Lanesra OS window",
  "windows": ["main"],
  "permissions": ["core:default"]
}
